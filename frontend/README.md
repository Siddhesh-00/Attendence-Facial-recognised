# Welcome to your Lovable project

## Project info

**URL**: https://lovable.dev/projects/REPLACE_WITH_PROJECT_ID

# Frontend

This folder contains the React + TypeScript frontend for the Smart Face Recognition Attendance System.

Quick start:

# Frontend

This folder contains the React + TypeScript frontend for the Smart Face Recognition Attendance System.

Quick start:

```sh
cd frontend
npm install
npm run dev
```

The frontend expects the backend API at `http://localhost:5000/api` by default (see `.env`).
